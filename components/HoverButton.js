import styles from "./HoverButton.module.css"; // 

const HoverButton = () => {
    return (

        <div>
            <button className={styles.btn}>
                GO!
            </button>
        </div>
    )
}

export default HoverButton;